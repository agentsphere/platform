apiVersion: v1
kind: ConfigMap
metadata:
  name: platform-config
data:
  PLATFORM_LISTEN: "0.0.0.0:8080"
  PLATFORM_LOG: "info"
  DATABASE_URL: "postgres://platform:dev@platform-db-rw:5432/platform_dev"
  VALKEY_URL: "redis://valkey-external:6379"
  MINIO_ENDPOINT: "http://minio-external:9000"
  MINIO_ACCESS_KEY: "platform"
  MINIO_SECRET_KEY: "devdevdev"
