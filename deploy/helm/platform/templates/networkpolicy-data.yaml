apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ include "platform.fullname" . }}-data-stores
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "platform.labels" . | nindent 4 }}
spec:
  # Apply to all pods in this namespace that are NOT the platform pod itself
  # (i.e., PostgreSQL, Valkey, MinIO subchart pods)
  podSelector:
    matchExpressions:
      - key: app.kubernetes.io/name
        operator: NotIn
        values:
          - {{ include "platform.name" . }}
  policyTypes:
    - Ingress
  ingress:
    # S58: Only allow traffic from the platform pod — not all pods in the namespace
    - from:
        - podSelector:
            matchLabels:
              app.kubernetes.io/name: {{ include "platform.name" . }}
