apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: platform

resources:
  - deployment.yaml
  - service.yaml
  - ingest-deployment.yaml
  - ingest-service.yaml
  - k8s-watcher-deployment.yaml
  - configmap.yaml
  - secret.yaml
  - rbac.yaml
