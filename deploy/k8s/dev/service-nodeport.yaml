apiVersion: v1
kind: Service
metadata:
  name: platform-external
  labels:
    app: platform
spec:
  type: NodePort
  selector:
    app: platform
  ports:
    - name: http
      port: 8080
      targetPort: http
      nodePort: 30080
