{
  "name": "platform-ui",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node build.mjs --dev",
    "build": "node build.mjs",
    "test": "npx playwright test"
  },
  "dependencies": {
    "dompurify": "^3.2.4",
    "marked": "^15.0.0",
    "preact": "^10.25.0",
    "preact-router": "^4.1.2"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "esbuild": "^0.27.5",
    "typescript": "^5.7.0"
  }
}
